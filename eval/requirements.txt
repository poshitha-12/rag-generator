# Extra dependencies for eval/run_eval.py only — deliberately not merged
# into the top-level requirements.txt: the app image (Dockerfile,
# docker-smoke) never needs a RAGAS judge, and every build shouldn't pay
# for a dependency only a manual `make eval` run uses.
#
# Pin verified against the app's own pins (langchain==0.3.7,
# langchain-anthropic==0.3.0): a newer ragas pulls an unconstrained
# `langchain`, which pip's resolver bumps to a langchain-core incompatible
# with langchain-anthropic==0.3.0. 0.2.15 resolves cleanly alongside them -
# recheck with a fresh `pip install -r requirements.txt -r eval/requirements.txt`
# before bumping either file.
ragas==0.2.15
